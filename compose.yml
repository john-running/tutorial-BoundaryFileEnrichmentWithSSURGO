services:
  app:
    build: .
    image: ssurgo-local
    ports:
      - "8080:8080"
    environment:
      - SQLITE_PATH=/app/data/app.db
      - R_FACTOR_TIF=/app/data/R-Factor_CONUS.tif
    volumes:
      - ./data:/app/data
